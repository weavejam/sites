{
  "name": "weavejam-sites",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "deploy": "turbo run deploy",
    "new-site": "pwsh scripts/new-site.ps1"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
